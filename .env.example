# QuickBite Food Delivery - Environment Configuration Template
# 
# SECURITY WARNING: Never commit the actual .env file to version control!
# 
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Generate secure passwords using: openssl rand -base64 32
# 3. Replace all empty values with your generated secrets
# 4. Verify .env is in .gitignore (it should be!)

# ============================================
# Database Configuration
# ============================================
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=   # REQUIRED: Generate with: openssl rand -base64 32
MONGO_PORT=27017
MONGO_EXPRESS_PORT=8081

# ============================================
# Cache Configuration
# ============================================
REDIS_PORT=6379
REDIS_COMMANDER_PORT=8082

# ============================================
# Message Broker Configuration
# ============================================
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=   # REQUIRED: Generate with: openssl rand -base64 32
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# ============================================
# User Service Configuration
# ============================================
USER_SERVICE_PORT=3001
USER_DB=user_service
JWT_SECRET=   # REQUIRED: Generate with: openssl rand -base64 64
JWT_EXPIRES_IN=7d

# ============================================
# Restaurant Service Configuration
# ============================================
RESTAURANT_SERVICE_PORT=3002
RESTAURANT_DB=restaurant_service

# ============================================
# Order Service Configuration
# ============================================
ORDER_SERVICE_PORT=3003
ORDER_DB=order_service

# ============================================
# Application Configuration
# ============================================
NODE_ENV=development
LOG_LEVEL=info

# ============================================
# Future Services (Uncomment as needed)
# ============================================
# ORDER_SERVICE_PORT=3003
# RESTAURANT_DB=restaurant_service
# ORDER_SERVICE_PORT=3003
# ORDER_DB=order_service
# DRIVER_SERVICE_PORT=3004
# DRIVER_DB=driver_service
# NOTIFICATION_SERVICE_PORT=3005
# API_GATEWAY_PORT=3000
